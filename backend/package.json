{
  "name": "kinyozismart-backend",
  "version": "1.0.0",
  "private": true,
  "description": "BarberOS backend API + static frontend server",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "db:migrate": "psql \"$DATABASE_URL\" -f ../db/schema.sql",
    "db:seed": "psql \"$DATABASE_URL\" -f ../db/seed.sql"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "pg": "^8.11.5"
  }
}
